#define LID_OPEN_DETECT 46

void setup() {
  Serial.begin(115200);
  pinMode(LID_OPEN_DETECT, INPUT);  
}

void loop() {
  int state = digitalRead(LID_OPEN_DETECT);

  if (state == HIGH) {
    Serial.println("LID CLOSED");
  } else {
    Serial.println("LID OPEN");
  }

  delay(500);
}
